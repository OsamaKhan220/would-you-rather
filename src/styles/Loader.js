const styles = (theme) => ({
  centerScreen: {
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
    textAlign: "center",
    minHeight: "100vh",
  },
  loader: {
    width: 100,
    height: 100,
    color: "#00bcd4",
  },
});

export default styles;
