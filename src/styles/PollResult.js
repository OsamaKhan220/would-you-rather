const styles = (theme) => ({
  root: {
    flex: 6,
    justifyContent: "center",
    alignItems: "center",
    textAlign: "center",
  },
  heading: {
    background: "#00bcd4",
    padding: 10,
  },
});

export default styles;
